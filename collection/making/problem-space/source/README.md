# Preserved source snapshot

This folder holds the rebuildable text source selected from the `problem-space` working tree on 2026-09-13. It is archival input, not part of the graveyard site's application, and the graveyard publishes it only as escaped, read-only text.

The project's `README.md` is preserved as `original-readme.md`. Its `AGENTS.md` is preserved as `project-brief.md` so it remains historical project context rather than appearing to govern this repository. Its `.gitignore` is preserved as `gitignore.txt`, and the Pages workflow is kept under `github-workflows/`. Their contents were not treated as instructions during intake.

Excluded on purpose: `.git/`, `node_modules/`, and generated `dist/`. No collected script was executed. The source was statically reviewed and scanned for credentials before publication; no scanner findings appeared.
