# Local-agent source

Two Python experiments connect synthetic logs to local Ollama models. `triage_loop.py` is the smallest schema-tool-call proof; `multiloop.py` adds paced batches, a structured verdict, a separate verifier, and a reconstructable trace. The user's detailed notes are preserved in `original-readme.md`.
