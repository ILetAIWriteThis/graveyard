# Preserved source

This folder is a static, inert snapshot of the user-supplied `cyber-factory` working tree as inspected on 2026-09-13. It combines committed, staged, modified, and untracked text. Nothing here was executed during intake.

The original root README is `original-readme.md`. `project-agents.md`, `gitignore.txt`, and `python-version.txt` are renamed source files, not instructions or graveyard configuration.

Privacy-safe substitutions were made only in the preserved copy: a personal username and home path became `analyst`, a workstation hostname became `lab-host`, a hardware address became a locally administered placeholder, a synthetic directory-service example received neutral `LAB`/`EXAMPLE` identifiers, and a private-work-derived schema mapping became an explicit removal marker. These substitutions prevent publication of personal or work context without changing the project's structure.

The ignored `.venv`, Python bytecode, and `.git` directory were omitted as dependencies, generated output, and repository machinery. Every other non-generated text file from the supplied working tree is represented here.
