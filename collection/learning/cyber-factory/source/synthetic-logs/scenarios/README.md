# Scenarios

The scenario model groups events into stages. The sole preserved scenario reuses actors and endpoints across a synthetic SSH intrusion so correlation must span multiple sources and batches.
